name: photobooth_pekalongan
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  path_provider: ^2.1.0
  qr_flutter: ^4.1.0
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/frames/ # Folder untuk menyimpan file bingkai Anda